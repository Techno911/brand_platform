// Единственное место для текстов платформы.
// Компоненты импортируют отсюда — это конфиг, а не разветвление логики.
//
// Tone-of-voice (голос Ольги Чирковой из INSIGHTS.md):
//  · короткая фраза, глагол + объект;
//  · ключевые слова: «Методология», «черновик», «редактор», «подпись собственника»,
//    «Портрет клиента», «Сессия с собственником», «тест клиента»;
//  · Claude — thinking partner, не автогенератор;
//  · без сленга, без восклицаний, без эмодзи, без англицизмов-сленга.
//
// Структура login-hero:
//  · taglines = сжатые 3 пункта методологии (порядок важен);
//  · subtitle = описание продукта одной фразой;
//  · version = номер релиза в формате vMAJOR.MINOR.PATCH.
export const PLATFORM = {
  name: 'Чирков & Партнёры',
  product: 'Бренд-платформа',
  // NB: используется как подпись под h1 на login + в sidebar-шапке как бренд-строка.
  subtitle:
    'Платформа ведёт вас от интервью с клиентами до утверждённого документа: ' +
    'легенда, ценности, миссия, позиционирование, месседж.',
  // Версия релиза. Авто-инкремент на `npm run build` (см. scripts/bump-version.mjs).
  // Схема: vМажор.Минор.Патч — каждая часть ОДНОЗНАЧНАЯ (0..9), при переполнении
  // каскадирует выше (v3.4.9 → v3.5.0 → v3.5.1 ... → v3.9.9 → v4.0.0).
  // Мажор = смена методологии (3→4 = радикально другой подход, «bp.v4 ≠ bp.v3»).
  // Минор = новый канон на стадии (добавили key_insight/top_praise = v3.3).
  // Патч = обычный build-bump.
  version: 'v3.5.5',
  // Короткое описание — не используется в login-hero после ре-текста, оставлено для других мест (meta-description, header tooltip).
  description:
    'Бренд-платформа по стандарту Чиркова. От первого интервью до подписи собственника — за 8–12 рабочих дней.',

  // Три нумерованных пункта на login-hero (01/02/03). Глагол + объект.
  taglines: [
    'Начинаете с голоса клиента, а не с фантазий',
    'Claude делает черновик — вы редактируете',
    '4 стадии → утверждённый документ',
  ],

  // Под hero — 3 факта отличия BP от «бренда за 5 минут в ChatGPT».
  // Дублируют taglines нумерацией на дизайне, но используются только если нужен второй блок.
  // После ре-текста login-hero показывает только taglines; features оставлены для обратной совместимости на других страницах.
  features: [
    'Claude — черновик. Вы — редактор. Собственник — подпись.',
    'Четыре стадии. Следующая открывается только после подписи предыдущей.',
    '8–12 дней работы вместо 4–6 недель. Без потерь на встречах.',
  ],

  copyright: `© ${new Date().getFullYear()} Чирков и Партнёры · Бренд-платформа`,
  faviconLetter: 'Ч',
};
